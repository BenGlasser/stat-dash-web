import React from 'react';
import { GET_MATCHES } from '../gql/queries/matches';

const MatchStatsCard = ({ puuid }) => {
    return (
        <div>

        </div>
    );
};

export default MatchStatsCard;
